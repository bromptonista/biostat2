## Standard Error: SE = SD / sqrt(n), n = sample size

## Confidence intevals to standard deviations
## For, two-sided 95%: (get width of conf. interval) => qnorm(0.025, lower.tail=FALSE) * 2
##  Then: SD = sqrt(n) * (upper limit - lower limit) / 3.92
##  n = sample size, 3.92 is width of conf. interval from standard normal

## Two sample tests - two independent groups
## (X - Y) / (S_p * sqrt(1/n_x + 1/n_y)), S_p pooled sd
## (S_p)^2 = ((n_1-1)^2 * S_1^2 + (n_2 - 1)^2 * S_2^2 + ... ) / ((n_1 - 1) + (n_2 - ) + ... )


